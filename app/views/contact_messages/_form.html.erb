<%= form_with(model: [contact_message, Comment.new], local: true) do |form| %>
  <div class="field">
    <%= form.text_area :body, placeholder: "Add a comment..." %>
  </div>

  <div class="actions">
    <%= form.submit "Add Comment", class: "custom-link" %>
  </div>
<% end %>
